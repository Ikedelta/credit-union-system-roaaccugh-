{
  "version": 2,
  "buildCommand": "npm run build --workspace=website",

  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
